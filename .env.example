# Public canonical site URL, used by the production build.
PUBLIC_SITE_URL=https://example.com

# Set only in the CMS OAuth gateway deployment, never commit real values.
GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
