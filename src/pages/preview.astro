---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="内容预览" description="技术报告文章的丰富预览样式">
  <div class="content-shell">
    <section class="preview-hero">
      <p class="eyebrow">REPORT · PREVIEW</p>
      <h1>让 Agent Memory 更快、更可控：从上下文预算开始</h1>
      <p class="lede">一篇技术报告的完整预览：它既呈现可复用的工程洞察，也清楚说明假设、权衡与下一步。</p>
      <div class="preview-meta"><span>2026 年 7 月 10 日</span><span>·</span><span>12 分钟阅读</span><span>·</span><a href="https://github.com/">GitHub ↗</a></div>
    </section>
    <div class="article-layout">
      <article class="prose">
        <h2 id="summary">摘要</h2>
        <p>Agent 的记忆系统通常被描述为“存储 + 检索”，但真实的性能问题来自这条路径的每一次复制、排序和上下文拼接。本文以一个可观察、可回退的最小实现为起点，讨论如何把记忆当作受预算约束的系统资源。</p>
        <aside class="insight-box"><strong>核心结论</strong>优先控制进入上下文的 token 预算，再优化向量检索的召回率；前者直接决定延迟上限与成本边界。</aside>
        <h2 id="problem">问题边界</h2>
        <p>当任务跨越多轮、多个工具调用时，记忆读写会迅速占据端到端延迟。一个可靠的系统至少应回答三个问题：哪些内容值得保留、何时需要召回、以及召回后可以替换多少当前上下文。</p>
        <h2 id="design">设计草图</h2>
        <p>我们把记忆划分为工作记忆、会话摘要与长期索引。每一层都有独立的大小限制与淘汰策略，而不是把所有历史无差别地塞回 prompt。</p>
        <pre class="code-block"><code>const budget = allocateContext(&#123;
  windowTokens: 32_768,
  systemTokens: 1_200,
  currentTaskTokens: 4_800,
  memoryRatio: 0.18,
&#125;);</code></pre>
        <h2 id="evaluation">如何验证</h2>
        <p>评估不只看命中率。还应记录召回耗时、实际进入上下文的 token 数、任务完成率，以及当记忆不可用时系统是否能平稳降级。</p>
        <h2 id="next">下一步</h2>
        <p>后续会补充可复现实验、数据格式与开源实现。所有结论都将保持可追溯：原始记录、报告版本与仓库提交相互链接。</p>
      </article>
      <aside class="article-side">
        <p class="eyebrow">ON THIS PAGE</p>
        <a href="#summary">摘要</a><a href="#problem">问题边界</a><a href="#design">设计草图</a><a href="#evaluation">如何验证</a><a href="#next">下一步</a>
      </aside>
    </div>
  </div>
</BaseLayout>
